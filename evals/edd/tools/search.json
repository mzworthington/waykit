{
  "name": "search",
  "description": "Write JavaScript to query Cloudflare OpenAPI spec.paths and find endpoints. Use before execute when the path is unknown. Do not guess undocumented paths.",
  "inputSchema": {
    "type": "object",
    "required": ["code"],
    "properties": {
      "code": {
        "type": "string",
        "description": "Short query (e.g. rum site_info list) or JavaScript that filters spec.paths for those tokens. Use only to discover an unknown path."
      }
    },
    "additionalProperties": false
  }
}
