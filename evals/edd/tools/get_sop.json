{
  "name": "get_sop",
  "description": "Return one SOP file body by stem name (e.g. conventional-commits, context-budget, subagent-launch). Use this when the user asks to open a kit SOP. Truncated if very long.",
  "inputSchema": {
    "type": "object",
    "required": ["name"],
    "properties": {
      "name": { "type": "string", "description": "SOP file stem without .md" }
    },
    "additionalProperties": false
  }
}
