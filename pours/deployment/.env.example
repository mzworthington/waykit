# Local-only SigNoz Postgres metastore password (operator machine Compose).
# Copy to a gitignored Compose env file beside compose.yaml, then set a unique
# URL-safe value:
#   openssl rand -hex 24
# Recreate the Postgres volume if you rotate this after the first cast.
# Do not commit the real value.
SIGNOZ_POSTGRES_PASSWORD=
